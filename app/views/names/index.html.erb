<h1>姓名の一覧</h1>

<%= link_to 'New Name', new_name_path %>

<%= link_to 'Absorb', :controller => 'names', :action => :absorb %>

<%= link_to 'Export', :controller => 'names', :action => :export %>

<%= link_to 'Import', :controller => 'names', :action => :import %>

<%= link_to 'DeleteAll', :controller => 'names', :action => :delete_all %>

<table>
  <tr>
    <th>Sei</th>
    <th>Mei</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @names.each do |name| %>
  <tr>
    <td><%= name.sei %></td>
    <td><%= name.mei %></td>
    <td><%= link_to 'Show', name %></td>
    <td><%= link_to 'Edit', edit_name_path(name) %></td>
    <td><%= link_to 'Destroy', name, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Name', new_name_path %>

