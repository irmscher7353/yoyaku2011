<%= form_for(@menuitem) do |f| %>
  <% if @menuitem.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@menuitem.errors.count, "error") %> prohibited this menuitem from being saved:</h2>

      <ul>
      <% @menuitem.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :release_id, 'リリース' %><br />
    <%= f.collection_select :release_id, Release.ordered, :id, :name %>
  </div>
  <div class="field">
    <%= f.label :product_id, '商品' %><br />
    <%= f.collection_select :product_id,
			Product.find_products_for_sale, :id, :label %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
